import {StyleSheet, Platform} from 'react-native';
import theme from '../styles/theme.style';

export default StyleSheet.create({
  Button: {
    fontWeight: 'bold',
    textAlign: 'right',
    color: '#F85F6A',
  },
  Text_Button: {fontWeight: 'bold', textAlign: 'left', color: '#989EB1'},
});
