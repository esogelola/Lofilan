import {DefaultTheme} from 'react-native-paper';

const mainTheme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: 'tomato',
    accent: 'yellow',
    background: 'white',
  },
};

export default mainTheme;
